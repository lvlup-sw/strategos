# Implementation Plan — v2.8.0 Close-Out

- **Design:** `docs/designs/2026-05-24-v2.8.0-closeout.md`
- **Feature ID:** `v2-8-0-closeout`
- **Date:** 2026-05-24
- **Iron Law:** NO PRODUCTION CODE WITHOUT A FAILING TEST FIRST
- **Test framework:** TUnit (`[Test]`, `await Assert.That(...)`). Invoke via `dotnet run` + `-- --treenode-filter "/*/*/*/Name"` (NOT `--filter`).

## Pipeline note (Contracts TDD shape)

For TypeSpec-sourced types the RED test asserts the **generated** C# record (`src/Strategos.Contracts/Generated/*.g.cs`) exists with the mandated INV-6/INV-7 shape; it fails to compile/resolve until the `.tsp` is authored, imported in `main.tsp`, and the codegen pipeline (`tsp compile` → `Strategos.Contracts.Codegen` `RecordEmitter`) regenerates. GREEN = author `.tsp` + import + regenerate. The `.tsp` is the production artifact under the failing test.

## PR / parallelization map

- **PR 1 — S1–S4 contract surface** (T1–T10): internally ordered S2→S3→S4→S1 (type graph). Branch `feat/v2.8.0-contracts-smq`.
- **PR 2 — drift/AGWF hardening** (T11–T14): independent of PR1/PR3. Branch `feat/v2.8.0-drift-hardening`.
- **PR 3 — Ontology MCP bridge** (T15–T18): independent. Branch `feat/v2.8.0-ontology-mcp-bridge`.

The three PRs are parallel-safe (no shared code). Within PR1, tasks are sequential by dependency.

---

## PR 1 — Cross-product contract surface (#64, #66, #65, #63)

### Task 1: S2 base response envelope `_meta` block (#64)
**Phase:** RED → GREEN → REFACTOR

1. [RED] `IntentEnvelopeMeta_BaseBlock_CarriesDegradedFields`
   - File: `src/Strategos.Contracts.Tests/Pipeline/IntentEnvelopeTests.cs`
   - Asserts generated `ResponseMetaV1` record exposes `Degraded` (bool) + `DegradedReason?`; `{ get; init; }`, sealed.
   - Expected failure: type does not exist.
2. [GREEN] Author `src/Strategos.Contracts/Events/IntentEnvelope.tsp` — `ResponseMetaV1 { degraded: boolean; degradedReason?: DegradedReason }` + shared `_perf` block; import in `main.tsp`; regenerate.
3. [REFACTOR] Doc-comment the consumer short-circuit rule (`degraded==true` ⇒ skip "use the decision").

**Dependencies:** None. **Parallelizable:** No (PR1 root).

### Task 2: `DegradedReason` closed enum (#64)
**Phase:** RED → GREEN

1. [RED] `DegradedReason_Enum_RoundTripsByName`
   - File: `src/Strategos.Contracts.Tests/Pipeline/IntentEnvelopeTests.cs`
   - Asserts members `DownstreamUnreachable`, `JudgeTimeout`, `MalformedOutput` (+ others) serialize **by name**.
   - Expected failure: enum absent.
2. [GREEN] Add `DegradedReason` enum to `IntentEnvelope.tsp`.

**Dependencies:** T1. **Parallelizable:** No.

### Task 3: `WorkflowRef` discriminated union (#66)
**Phase:** RED → GREEN → REFACTOR

1. [RED] `WorkflowRef_CatalogVariant_CarriesWorkflowIdAndCatalogVersion` + `WorkflowRef_AuthoredVariant_CarriesJourneyDescription`
   - File: `src/Strategos.Contracts.Tests/Pipeline/WorkflowRefTests.cs`
   - Asserts discriminator `kind`: `"catalog"`→`WorkflowId`+`CatalogVersion`; `"authored"`→`JourneyDescription`.
   - Expected failure: type absent.
2. [GREEN] Author `src/Strategos.Contracts/Workflow/WorkflowRef.tsp` (discriminated union, INV-8 monikers — string ids, no CLR type); import in `main.tsp`; regenerate.
3. [REFACTOR] Mark `authored` operationally-deferred in doc comment (basileus #179 / exarchos #1255).

**Dependencies:** None (independent of T1–T2). **Parallelizable:** Yes (within PR1, vs T1).

### Task 4: `WorkflowCatalog` manifest + entries (#65)
**Phase:** RED → GREEN

1. [RED] `WorkflowCatalog_Manifest_CarriesVersionAndEntries`
   - File: `src/Strategos.Contracts.Tests/Pipeline/WorkflowCatalogTests.cs`
   - Asserts manifest carries `CatalogVersion` + `IReadOnlyList<WorkflowIrEntry>`.
   - Expected failure: type absent.
2. [GREEN] Author `src/Strategos.Contracts/Workflow/WorkflowCatalog.tsp` (manifest + array of `WorkflowIR` entries reusing `WorkflowDefinitionV1`); import; regenerate.

**Dependencies:** None. **Parallelizable:** Yes.

### Task 5: catalog-ref validation at consumer time (#65/#66)
**Phase:** RED → GREEN → REFACTOR

1. [RED] `CatalogRef_ValidatesAgainstManifest_RejectsUnknownVersion`
   - File: `src/Strategos.Contracts.Tests/Pipeline/WorkflowCatalogTests.cs`
   - Asserts a `WorkflowRef.catalog` with an absent `(workflow_id, catalog_version)` fails a `WorkflowCatalog.Resolve(ref)` check; present pair resolves.
   - Expected failure: resolver absent.
2. [GREEN] Hand-authored resolver helper `src/Strategos.Contracts/WorkflowCatalogResolver.cs` (sealed, pure; INV-6/INV-7).
3. [REFACTOR] Surface a typed result, not a bool, if call sites need the entry.

**Dependencies:** T3, T4. **Parallelizable:** No.

### Task 6: `MergeGateDecision` envelope (#63)
**Phase:** RED → GREEN

1. [RED] `MergeGateDecision_ExtendsBaseMeta_AndTypesSuggestedJourneys`
   - File: `src/Strategos.Contracts.Tests/Pipeline/MergeGateDecisionTests.cs`
   - Asserts record carries `_meta` (ResponseMetaV1), `_perf`, and `SuggestedJourneys: IReadOnlyList<WorkflowRef>`.
   - Expected failure: type absent.
2. [GREEN] Author `src/Strategos.Contracts/Events/MergeGateDecision.tsp` extending the S2 base; import; regenerate.

**Dependencies:** T1, T2, T3. **Parallelizable:** No.

### Task 7: `JourneyResult` envelope (#63)
**Phase:** RED → GREEN

1. [RED] `JourneyResult_ExtendsBaseMeta_AndTypesJourneyOutcomes`
   - File: `src/Strategos.Contracts.Tests/Pipeline/JourneyResultTests.cs`
   - Asserts `JourneyOutcomes: IReadOnlyList<WorkflowRef>` + base `_meta`/`_perf`.
   - Expected failure: type absent.
2. [GREEN] Author `src/Strategos.Contracts/Events/JourneyResult.tsp`; import; regenerate.

**Dependencies:** T1, T2, T3. **Parallelizable:** Yes (vs T6).

### Task 8: discriminated `next_actions` pattern (#63)
**Phase:** RED → GREEN

1. [RED] `NextActions_DiscriminatedUnion_TypedBranches`
   - File: `src/Strategos.Contracts.Tests/Pipeline/MergeGateDecisionTests.cs`
   - Asserts `NextActions` is a discriminated union (typed branches, no string-typed indirection — DIM-3).
   - Expected failure: shape absent.
2. [GREEN] Add `NextAction` union to the S1 `.tsp`; wire into both envelopes.

**Dependencies:** T6, T7. **Parallelizable:** No.

### Task 9: cross-product round-trip over the 4 new types (#63/#64/#65/#66)
**Phase:** RED → GREEN → REFACTOR

1. [RED] `CrossProductRoundTrip_SmqEnvelopes_ValidateBothDirections`
   - File: `src/Strategos.Contracts.Tests/Pipeline/CrossProductRoundTripTests.cs`
   - Extends the existing harness: a C# binding serializes → JSON validates against emitted JSON Schema; a fixture parses back. DIM-4: construct bindings the way consumers do.
   - Expected failure: new types not in fixture corpus / `.mjs` set.
2. [GREEN] Add fixtures (extend the #53 corpus) + register the 4 schemas in `src/Strategos.Contracts/scripts/cross-product-roundtrip.mjs`.
3. [REFACTOR] Dedup fixture-loading helper if duplicated.

**Dependencies:** T5, T8. **Parallelizable:** No.

### Task 10: packaging — schemas embedded as NuGet content
**Phase:** RED → GREEN

1. [RED] `Packaging_SmqSchemas_EmbeddedAsContent`
   - File: `src/Strategos.Contracts.Tests/PackagingTests.cs`
   - Asserts the 4 new `*.json` schemas ship in the package content (exarchos derives Zod).
   - Expected failure: not packaged.
2. [GREEN] Ensure build globs the new schemas into content (likely automatic; confirm csproj glob).

**Dependencies:** T9. **Parallelizable:** No.

---

## PR 2 — Drift / AGWF hardening (#105, #106, #107, close #54)

### Task 11: AGWF catalog metadata-parity test (#105)
**Phase:** RED → GREEN → REFACTOR

1. [RED] `AgwfCatalog_MetadataParity_MatchesLiveDescriptors`
   - File: `src/Strategos.Generators.Tests/Diagnostics/AgwfCatalogParityTests.cs`
   - For each of the 10 live codes, assert generated `agwf-catalog.json` `severity`/`summary`/`remediation` equals the `WorkflowDiagnostics` descriptor `defaultSeverity`/`title`/`messageFormat`.
   - Expected failure: no such test (and surfaces any existing drift).
2. [GREEN] Add the test; wire `Strategos.Generators.Tests` access to the generated catalog (content file or `Contracts` reference). Fix any real mismatch found in `AgwfCatalog.tsp`.
3. [REFACTOR] Table-drive over the 10 codes.

**Dependencies:** None. **Parallelizable:** Yes.

### Task 12: drift-issue label fail-open (#107)
**Phase:** RED → GREEN

1. [RED] `BuildDriftIssue_CreatesLabelIdempotently_BeforeIssueCreate`
   - File: `src/Strategos.Generators.Tests/Diagnostics/DriftIssueScriptTests.cs` (or a `bats`/shell guard if scripts are tested that way in-repo)
   - Asserts `scripts/build-drift-issue.sh` runs `gh label create cross-product:strategos … || true` **before** `gh issue create`.
   - Expected failure: script lacks the idempotent create.
2. [GREEN] Edit `scripts/build-drift-issue.sh`: idempotent label create before issue create.

**Dependencies:** None. **Parallelizable:** Yes.

### Task 13: api-mirror scope — gate the 5 ungated continuations OR document boundary (#106)
**Phase:** RED → GREEN (evidence-gated decision)

1. [DECISION] Confirm whether exarchos `strategos-api-mirror.test.ts` parses only the 7 entrypoints or the transitive type graph (read the exarchos test; this is the gate input).
2. [RED — if transitive] `PublicApiGate_CoversTransitiveContinuations` asserting `IForkPathBuilder`, `ILoopForkJoinBuilder`, `IApprovalEscalationBuilder`, `IApprovalRejectionBuilder`, `IContextBuilder` are in the gated baseline.
   - Expected failure: 5 interfaces ungated.
   [GREEN] Add them to the `PublicAPI.Shipped.txt` baseline (expand gated set).
3. [DOC — if only 7] Document the boundary in `.github/workflows/public-api-drift.yml` header + a `PublicApiGate_BoundaryDocumented` assertion that the note exists.

**Dependencies:** None. **Parallelizable:** Yes (decision-gated).

### Task 14: verify + close slice-B sub-tracker (#54)
**Phase:** Administrative (no production code — Iron Law N/A)

1. Verify #50/#51/#52/#53 closed; confirm exarchos cross-product round-trip + `strategos-api-mirror.test.ts` green against the 0.2.0 baseline (external check).
2. Close #54 with a verification note linking the close-out PRs.

**Dependencies:** T9 (round-trip evidence), T13. **Parallelizable:** No.

---

## PR 3 — Ontology MCP registration bridge (#104)

### Task 15: descriptor → `McpServerTool` adapter preserving schema + annotations
**Phase:** RED → GREEN → REFACTOR

1. [RED] `CreateServerTools_PreservesOutputSchemaAndAnnotations`
   - File: `src/Strategos.Ontology.MCP.Hosting.Tests/OntologyServerToolFactoryTests.cs`
   - Asserts each `McpServerTool` from `CreateServerTools(graph)` carries the descriptor's `OutputSchema` + `ToolAnnotations` + `ConstraintSummaries` (verified preserved through `McpServerTool.Create`, MCP SDK 1.3.0).
   - Expected failure: factory absent.
2. [GREEN] New companion package `src/Strategos.Ontology.MCP.Hosting/` (references `ModelContextProtocol` SDK): `OntologyServerToolFactory.CreateServerTools(OntologyGraph)` → `IEnumerable<McpServerTool>` via `OntologyToolDiscovery(graph).Discover()` + `McpServerTool.Create(...)`.
3. [REFACTOR] Centralize annotation mapping.

**Dependencies:** None. **Parallelizable:** Yes.

### Task 16: `IMcpServerBuilder.AddOntologyTools(graph)` extension (#104)
**Phase:** RED → GREEN

1. [RED] `AddOntologyTools_RegistersFourCallableTools`
   - File: `src/Strategos.Ontology.MCP.Hosting.Tests/AddOntologyToolsTests.cs`
   - Asserts after `AddOntologyTools(graph)`, `tools/list` advertises `ontology_explore/query/action/validate` and `tools/call` for `ontology_query` routes (in-memory MCP client/server — DIM-4 integration).
   - Expected failure: extension absent.
2. [GREEN] `IMcpServerBuilder.AddOntologyTools(this, graph)` wrapping `WithTools(IEnumerable<McpServerTool>)`.

**Dependencies:** T15. **Parallelizable:** No.

### Task 17: core assembly stays SDK-free (INV-2)
**Phase:** RED → GREEN

1. [RED] `CoreMcpAssembly_HasNoModelContextProtocolDependency`
   - File: `src/Strategos.Ontology.MCP.Tests/AssemblyDependencyTests.cs`
   - Asserts `Strategos.Ontology.MCP` referenced assemblies do **not** include `ModelContextProtocol` (INV-2 — library, not server; adapter lives in `.Hosting`).
   - Expected failure: if any SDK type leaked into core.
2. [GREEN] Ensure all SDK-typed code is in `.Hosting`; core continues emitting descriptors only.

**Dependencies:** T15. **Parallelizable:** No.

### Task 18: companion package wiring + packaging
**Phase:** RED → GREEN

1. [RED] `Packaging_HostingPackage_ShipsAdapterAndExtension`
   - File: `src/Strategos.Ontology.MCP.Hosting.Tests/PackagingTests.cs`
   - Asserts the `.Hosting` package ships the factory + extension and depends on the MCP SDK + core `Strategos.Ontology.MCP`.
   - Expected failure: package not in solution / not wired.
2. [GREEN] Add `Strategos.Ontology.MCP.Hosting` to the solution, `Directory.Packages.props`, packaging metadata.

**Dependencies:** T16. **Parallelizable:** No.

---

## Out of scope (defer)

- **#100** bidirectional `FromContract()` — future milestone (moniker→Type resolver + lambda rebind).
- `WorkflowRef.authored` operational path — type defined (T3), no consumer yet.

## Summary

18 tasks across 3 PRs. PR1 (T1–T10) sequential by type graph. PR2 (T11–T14) and PR3 (T15–T18) independent and parallel-safe. #54 (T14) and #106 (T13) include non-TDD verify/decision steps. #100 deferred.
