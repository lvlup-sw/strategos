---
name: design-invariants
description: "Audit a design proposal or diff against Strategos's architectural invariants — workflows lower to Wolverine+Marten via Roslyn SG, ontology is analyzer-validated and self-contained, MCP tracks the latest protocol spec (2026-07-28), the workflow DSL uses concrete domain nomenclature, state is immutable, types are sealed-by-default, and descriptor identity is polyglot. Pairs with /axiom:design — this skill is project-specific (axiom is generic). Triggers: 'check invariants', 'design conformance', or /design-invariants."
metadata:
  author: strategos
  version: 0.1.0
  category: review
  pairs-with: axiom:design
---

# Strategos Design Invariants

Audits a design proposal or diff against Strategos-specific architectural invariants. Project-scoped — these invariants govern Strategos itself, not consumers of it.

## Project framing

> Fluent DSL for building durable agentic workflows and ontologies in .NET.

This sentence governs every invariant below. When a candidate constraint doesn't follow from this characterization, the framing rejects it.

## When to use

- During design (`/exarchos:ideate`, RFC drafting), before committing.
- During review (`/exarchos:review`), alongside `/axiom:audit`.
- When reviewing a PR that touches the workflow DSL/builder surface (`src/Strategos/`), the Roslyn generators (`src/Strategos.Generators/`), the ontology descriptor model (`src/Strategos.Ontology/`), or the MCP protocol layer (`src/Strategos.Ontology.MCP/`).

## When NOT to use

- For generic backend quality — use `/axiom:*` skills.
- For TDD / spec compliance — use the workflow's `review` skill.

## Invariant references

- [**U-1**](../../../docs/architecture/invariants/U-1-workflow-durability-via-wolverine-marten.md) — Workflows lower into Wolverine + Marten via Roslyn SG. **HIGH** · `axiom_overlap: DIM-6` (also DIM-1)
- [**U-2**](../../../docs/architecture/invariants/U-2-ontology-analyzer-only-self-contained.md) — Ontology uses Roslyn **analyzers** (not generators) and is self-contained. **HIGH** · `axiom_overlap: DIM-6` (also DIM-3)
- [**U-3**](../../../docs/architecture/invariants/U-3-mcp-first-class-latest-spec.md) — MCP is first-class and tracks the latest protocol spec (currently 2026-07-28). **MEDIUM** · `axiom_overlap: DIM-3` (also DIM-6)
- [**U-4**](../../../docs/architecture/invariants/U-4-concrete-dsl-nomenclature.md) — Fluent workflow DSL uses concrete domain nomenclature, never abstract structural terms. **MEDIUM** · `axiom_overlap: DIM-3` (also DIM-8)
- [**U-5**](../../../docs/architecture/invariants/U-5-three-tiered-validation-stable-diagnostic-ids.md) — Three-tiered validation with stable diagnostic IDs (`AGWF*` / `AONT*`). **HIGH** · `axiom_overlap: DIM-7` (also DIM-3, DIM-2)
- [**U-6**](../../../docs/architecture/invariants/U-6-sealed-by-default.md) — Sealed-by-default for DSL and descriptor types. **HIGH** · `axiom_overlap: DIM-6` (also DIM-3)
- [**U-7**](../../../docs/architecture/invariants/U-7-immutable-record-state.md) — Immutable record state; step results never mutate input. **HIGH** · `axiom_overlap: DIM-7` (also DIM-3, DIM-4)
- [**U-8**](../../../docs/architecture/invariants/U-8-polyglot-identity.md) — Polyglot identity: `ClrType` OR `SymbolKey`, both first-class. **HIGH** · `axiom_overlap: DIM-3` (also DIM-1, DIM-6)

See [`deterministic-checks.md`](../../../docs/architecture/invariants/deterministic-checks.md) for mechanical grep / structural patterns per invariant. The canonical catalog lives in [`docs/architecture/invariants/`](../../../docs/architecture/invariants/README.md) with its machine-readable twin at [`.exarchos/invariants.md`](../../../.exarchos/invariants.md); ids were INV-N until 2026-09-10 and are U-N now (exarchos reserves `INV-*`).

## Finding format

Match axiom's vocabulary (HIGH / MEDIUM / LOW) so reviewers don't context-switch:

```json
{
  "verdict": "pass | conditional | fail",
  "findings": [
    {
      "invariant": "U-1",
      "severity": "HIGH",
      "file": "path/to/file.cs",
      "line": 42,
      "description": "...",
      "required_fix": "...",
      "axiom_overlap": "DIM-6"
    }
  ]
}
```

## Pairing with axiom

The seam: axiom asks *"is this code well-engineered?"*; this skill asks *"does this design respect Strategos's load-bearing invariants?"* A design can be axiom-clean and still violate a project invariant.

For the full pairing protocol, see the axiom plugin's pairing contract at [`@skills/backend-quality/references/pairing-contract.md`](https://github.com/lvlup-sw/axiom/blob/main/skills/backend-quality/references/pairing-contract.md) (GitHub link; resolves regardless of where this generated skill is installed).

## Source

This skill was generated by `/axiom:scaffold-invariants` on 2026-05-15 and re-pointed at `docs/architecture/invariants/` on 2026-09-10. Amend the catalog files (prose + `.exarchos/invariants.md`) when invariants change; this file only indexes them.
