// <auto-generated/>
#nullable enable

using System;
using System.Collections.Generic;
using Strategos.Identity.Abstractions;
using Strategos.Services;
using Marten.Schema;
using Microsoft.Extensions.Logging;
using Wolverine;
using Wolverine.Persistence.Sagas;
using TestNamespace;

namespace TestNamespace;

/// <summary>
/// Wolverine saga for the process-order workflow.
/// </summary>
[global::System.CodeDom.Compiler.GeneratedCode("LevelUp.Strategos", "{generator-version}")]
[global::System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]
public partial class ProcessOrderSaga : Saga, IPhaseAwareSaga
{
    /// <summary>
    /// Gets or sets the workflow identifier.
    /// </summary>
    [SagaIdentity]
    [JasperFx.Identity]
    public Guid WorkflowId { get; set; }

    /// <summary>
    /// Gets or sets the version for optimistic concurrency control.
    /// </summary>
    [Version]
    public new long Version { get; set; }

    /// <summary>
    /// Gets or sets the current workflow phase.
    /// </summary>
    public ProcessOrderPhase Phase { get; set; } = ProcessOrderPhase.NotStarted;

    /// <summary>
    /// Gets the current saga phase as a stable string identifier (Phase.ToString()).
    /// </summary>
    public string CurrentPhaseName => Phase.ToString();

    /// <summary>
    /// Gets or sets the workflow state.
    /// </summary>
    public OrderState State { get; set; } = default!;

    /// <summary>
    /// Gets or sets the timestamp when the workflow started.
    /// </summary>
    public DateTimeOffset StartedAt { get; set; }
    /// <summary>
    /// Starts a new process-order workflow.
    /// </summary>
    /// <param name="command">The start workflow command.</param>
    /// <returns>A tuple containing the saga instance and the first step command.</returns>
    public static (ProcessOrderSaga Saga, StartValidateOrderCommand Command) Start(
        StartProcessOrderCommand command)
    {
        ArgumentNullException.ThrowIfNull(command, nameof(command));

        var saga = new ProcessOrderSaga
        {
            WorkflowId = command.WorkflowId,
            Phase = ProcessOrderPhase.NotStarted,
            State = command.InitialState,
            StartedAt = DateTimeOffset.UtcNow
        };

        var stepCommand = new StartValidateOrderCommand(command.WorkflowId);

        return (saga, stepCommand);
    }

    /// <summary>
    /// Handles the StartValidateOrderCommand - transitions phase and dispatches to worker.
    /// </summary>
    /// <param name="command">The start ValidateOrder command.</param>
    /// <returns>The worker command to execute the step.</returns>
    public ExecuteValidateOrderWorkerCommand Handle(
        StartValidateOrderCommand command,
        ILogger<ProcessOrderSaga> logger)
    {
        ArgumentNullException.ThrowIfNull(command, nameof(command));
        ArgumentNullException.ThrowIfNull(logger, nameof(logger));

        Phase = ProcessOrderPhase.ValidateOrder;

        logger.LogDebug(
            "Dispatching {CommandType} for workflow {WorkflowId}",
            nameof(ExecuteValidateOrderWorkerCommand),
            WorkflowId);

        return new ExecuteValidateOrderWorkerCommand(WorkflowId, Guid.NewGuid(), State);
    }

    /// <summary>
    /// Handles the ValidateOrderCompleted event - applies state change and chains to next step.
    /// </summary>
    /// <param name="evt">The ValidateOrder completed event.</param>
    /// <returns>The command to start the next step.</returns>
    public IEnumerable<object> Handle(
        ValidateOrderCompleted evt,
        ILogger<ProcessOrderSaga> logger)
    {
        ArgumentNullException.ThrowIfNull(evt, nameof(evt));
        ArgumentNullException.ThrowIfNull(logger, nameof(logger));

        State = OrderStateReducer.Reduce(State, evt.UpdatedState);

        logger.LogDebug(
            "Step completed, chaining to {NextStep} for workflow {WorkflowId}",
            nameof(StartProcessPaymentCommand),
            WorkflowId);

        yield return new StartProcessPaymentCommand(WorkflowId);
    }

    /// <summary>
    /// Handles the StartProcessPaymentCommand - transitions phase and dispatches to worker.
    /// </summary>
    /// <param name="command">The start ProcessPayment command.</param>
    /// <returns>The worker command to execute the step.</returns>
    public ExecuteProcessPaymentWorkerCommand Handle(
        StartProcessPaymentCommand command,
        ILogger<ProcessOrderSaga> logger)
    {
        ArgumentNullException.ThrowIfNull(command, nameof(command));
        ArgumentNullException.ThrowIfNull(logger, nameof(logger));

        Phase = ProcessOrderPhase.ProcessPayment;

        logger.LogDebug(
            "Dispatching {CommandType} for workflow {WorkflowId}",
            nameof(ExecuteProcessPaymentWorkerCommand),
            WorkflowId);

        return new ExecuteProcessPaymentWorkerCommand(WorkflowId, Guid.NewGuid(), State);
    }

    /// <summary>
    /// Handles the ProcessPaymentCompleted event - applies state change and chains to next step.
    /// </summary>
    /// <param name="evt">The ProcessPayment completed event.</param>
    /// <returns>The command to start the next step.</returns>
    public IEnumerable<object> Handle(
        ProcessPaymentCompleted evt,
        ILogger<ProcessOrderSaga> logger)
    {
        ArgumentNullException.ThrowIfNull(evt, nameof(evt));
        ArgumentNullException.ThrowIfNull(logger, nameof(logger));

        State = OrderStateReducer.Reduce(State, evt.UpdatedState);

        logger.LogDebug(
            "Step completed, chaining to {NextStep} for workflow {WorkflowId}",
            nameof(StartSendConfirmationCommand),
            WorkflowId);

        yield return new StartSendConfirmationCommand(WorkflowId);
    }

    /// <summary>
    /// Handles the StartSendConfirmationCommand - transitions phase and dispatches to worker.
    /// </summary>
    /// <param name="command">The start SendConfirmation command.</param>
    /// <returns>The worker command to execute the step.</returns>
    public ExecuteSendConfirmationWorkerCommand Handle(
        StartSendConfirmationCommand command,
        ILogger<ProcessOrderSaga> logger)
    {
        ArgumentNullException.ThrowIfNull(command, nameof(command));
        ArgumentNullException.ThrowIfNull(logger, nameof(logger));

        Phase = ProcessOrderPhase.SendConfirmation;

        logger.LogDebug(
            "Dispatching {CommandType} for workflow {WorkflowId}",
            nameof(ExecuteSendConfirmationWorkerCommand),
            WorkflowId);

        return new ExecuteSendConfirmationWorkerCommand(WorkflowId, Guid.NewGuid(), State);
    }

    /// <summary>
    /// Handles the SendConfirmationCompleted event - applies state change and chains to next step.
    /// </summary>
    /// <param name="evt">The SendConfirmation completed event.</param>
    public void Handle(
        SendConfirmationCompleted evt,
        ILogger<ProcessOrderSaga> logger)
    {
        ArgumentNullException.ThrowIfNull(evt, nameof(evt));
        ArgumentNullException.ThrowIfNull(logger, nameof(logger));

        State = OrderStateReducer.Reduce(State, evt.UpdatedState);
        Phase = ProcessOrderPhase.Completed;

        logger.LogInformation(
            "Workflow {WorkflowId} completed",
            WorkflowId);

        MarkCompleted();
    }
    /// <summary>
    /// Handles start command when saga no longer exists.
    /// </summary>
    public static void NotFound(StartProcessOrderCommand command, ILogger<ProcessOrderSaga> logger)
    {
        ArgumentNullException.ThrowIfNull(command, nameof(command));
        ArgumentNullException.ThrowIfNull(logger, nameof(logger));

        logger.LogWarning(
            "Received StartProcessOrderCommand for completed/unknown workflow {WorkflowId}",
            command.WorkflowId);
    }

    /// <summary>
    /// Handles ValidateOrderCompleted event when saga no longer exists.
    /// </summary>
    public static void NotFound(ValidateOrderCompleted evt, ILogger<ProcessOrderSaga> logger)
    {
        ArgumentNullException.ThrowIfNull(evt, nameof(evt));
        ArgumentNullException.ThrowIfNull(logger, nameof(logger));

        logger.LogWarning(
            "Received ValidateOrderCompleted for completed/unknown workflow {WorkflowId}",
            evt.WorkflowId);
    }

    /// <summary>
    /// Handles ProcessPaymentCompleted event when saga no longer exists.
    /// </summary>
    public static void NotFound(ProcessPaymentCompleted evt, ILogger<ProcessOrderSaga> logger)
    {
        ArgumentNullException.ThrowIfNull(evt, nameof(evt));
        ArgumentNullException.ThrowIfNull(logger, nameof(logger));

        logger.LogWarning(
            "Received ProcessPaymentCompleted for completed/unknown workflow {WorkflowId}",
            evt.WorkflowId);
    }

    /// <summary>
    /// Handles SendConfirmationCompleted event when saga no longer exists.
    /// </summary>
    public static void NotFound(SendConfirmationCompleted evt, ILogger<ProcessOrderSaga> logger)
    {
        ArgumentNullException.ThrowIfNull(evt, nameof(evt));
        ArgumentNullException.ThrowIfNull(logger, nameof(logger));

        logger.LogWarning(
            "Received SendConfirmationCompleted for completed/unknown workflow {WorkflowId}",
            evt.WorkflowId);
    }
}
