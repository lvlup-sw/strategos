{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "AgwfEntryCompensationScopeNotDerivable.json",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "const": "AGWF045"
        },
        "severity": {
            "type": "string",
            "const": "error"
        },
        "summary": {
            "type": "string",
            "const": "Compensation scope is not mechanically derivable"
        },
        "remediation": {
            "type": "string",
            "const": "Workflow '{0}' cannot derive rollback scope '{1}': step '{2}' is not compensable ({3}). Give every rollback-reachable forward occurrence a closed, proven inverse action."
        },
        "since": {
            "type": "string",
            "const": "2.13.0"
        }
    },
    "required": [
        "id",
        "severity",
        "summary",
        "remediation",
        "since"
    ],
    "description": "AGWF045 — A rollback scope contains a leaf whose inverse cannot be derived (#169)."
}