{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "AgwfEntryMalformedWorkflowJson.json",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "const": "AGWF023"
        },
        "severity": {
            "type": "string",
            "const": "error"
        },
        "summary": {
            "type": "string",
            "const": "Malformed workflow import JSON"
        },
        "remediation": {
            "type": "string",
            "const": "Workflow import file '{0}' is not well-formed JSON and was skipped: {1}"
        },
        "since": {
            "type": "string",
            "const": "2.10.0"
        }
    },
    "required": [
        "id",
        "severity",
        "summary",
        "remediation",
        "since"
    ],
    "description": "AGWF023 — Malformed workflow import JSON (DR-12)."
}