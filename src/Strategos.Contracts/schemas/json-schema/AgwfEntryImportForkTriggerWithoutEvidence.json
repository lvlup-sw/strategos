{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "AgwfEntryImportForkTriggerWithoutEvidence.json",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "const": "AGWF034"
        },
        "severity": {
            "type": "string",
            "const": "error"
        },
        "summary": {
            "type": "string",
            "const": "Imported fork trigger declares no required evidence fields"
        },
        "remediation": {
            "type": "string",
            "const": "Workflow import file '{0}' declares a diagnostic-fork permitted trigger at {1} (trigger '{2}') with no required evidence fields. A permitted fork trigger must declare at least one required evidence field (wire @minItems(1)) so the DR-8 no-unjustified-fork guard has an evidence floor to enforce; the workflow is rejected and no saga is generated. Add the evidence field(s) the trigger requires."
        },
        "since": {
            "type": "string",
            "const": "2.10.0"
        }
    },
    "required": [
        "id",
        "severity",
        "summary",
        "remediation",
        "since"
    ],
    "description": "AGWF034 — Imported fork trigger declares no required evidence fields (DR-8)."
}