{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "StructuralLeaf.json",
    "type": "object",
    "properties": {
        "kind": {
            "type": "string",
            "const": "structural"
        },
        "pattern": {
            "type": "string",
            "description": "The structural pattern to match; inert match data, not a command."
        },
        "file-glob": {
            "type": "string",
            "description": "Optional file glob scoping the match (kebab-case wire name)."
        }
    },
    "required": [
        "kind",
        "pattern"
    ],
    "description": "Leaf: a `structural` check — a pattern match interpreted structurally (e.g.\nover an AST or symbol table) rather than line-by-line. Declarative-only: the\n`pattern`/`file-glob` are match data, never executable code (LB-1 / INV-4)."
}