{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "StructuralLeaf.json",
    "type": "object",
    "properties": {
        "kind": {
            "type": "string",
            "const": "structural"
        },
        "pattern": {
            "type": "string"
        },
        "file-glob": {
            "type": "string"
        },
        "threshold": {
            "type": "number"
        }
    },
    "required": [
        "kind",
        "pattern"
    ],
    "description": "Declarative structural match. Pattern and glob are inert data; threshold is consumer-interpreted.",
    "additionalProperties": false
}