{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "AgwfEntryDuplicateStepName.json",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "const": "AGWF003"
        },
        "severity": {
            "type": "string",
            "const": "error"
        },
        "summary": {
            "type": "string",
            "const": "Duplicate step name"
        },
        "remediation": {
            "type": "string",
            "const": "Step '{0}' appears multiple times in workflow '{1}'. Each step type should be unique."
        },
        "since": {
            "type": "string",
            "const": "0.2.0"
        }
    },
    "required": [
        "id",
        "severity",
        "summary",
        "remediation",
        "since"
    ],
    "description": "AGWF003 — Duplicate step name."
}