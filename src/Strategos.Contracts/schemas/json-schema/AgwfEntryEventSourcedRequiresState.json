{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "AgwfEntryEventSourcedRequiresState.json",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "const": "AGWF016"
        },
        "severity": {
            "type": "string",
            "const": "error"
        },
        "summary": {
            "type": "string",
            "const": "Event-sourced workflow requires state type"
        },
        "remediation": {
            "type": "string",
            "const": "Workflow '{0}' uses PersistenceMode.EventSourced but no state type was found. Event-sourced workflows require a state type that implements IEventSourcedState<TState> with an ApplyEvent method."
        },
        "since": {
            "type": "string",
            "const": "0.2.0"
        }
    },
    "required": [
        "id",
        "severity",
        "summary",
        "remediation",
        "since"
    ],
    "description": "AGWF016 — Event-sourced workflow requires state type."
}