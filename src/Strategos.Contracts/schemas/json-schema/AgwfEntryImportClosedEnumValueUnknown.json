{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "AgwfEntryImportClosedEnumValueUnknown.json",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "const": "AGWF049"
        },
        "severity": {
            "type": "string",
            "const": "error"
        },
        "summary": {
            "type": "string",
            "const": "Closed-enum wire slot carries an unknown value"
        },
        "remediation": {
            "type": "string",
            "const": "Import file '{0}' declares {1} at {2}. A closed enum is not an extension point — both runtimes match {3} BY VALUE, so check for a member name used in place of its wire value (PascalCase for snake_case), or a class retired in an earlier contract major. The {3} vocabulary is exactly: {4}."
        },
        "since": {
            "type": "string",
            "const": "3.0.0"
        }
    },
    "required": [
        "id",
        "severity",
        "summary",
        "remediation",
        "since"
    ],
    "description": "AGWF049 — A wire slot typed by a closed enum carries a value outside its vocabulary (#221)."
}