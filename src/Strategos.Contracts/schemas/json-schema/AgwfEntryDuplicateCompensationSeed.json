{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "AgwfEntryDuplicateCompensationSeed.json",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "const": "AGWF038"
        },
        "severity": {
            "type": "string",
            "const": "error"
        },
        "summary": {
            "type": "string",
            "const": "Duplicate diagnostic-fork compensation seed"
        },
        "remediation": {
            "type": "string",
            "const": "Workflow '{0}' declares compensation seed '{2}' more than once on diagnostic-fork edges at {1}. Two edges that share a seed cannot share a DiagnosticForkCount counter; use distinct compensation seeds."
        },
        "since": {
            "type": "string",
            "const": "2.11.0"
        }
    },
    "required": [
        "id",
        "severity",
        "summary",
        "remediation",
        "since"
    ],
    "description": "AGWF038 — Duplicate diagnostic-fork compensation seed (#156.3)."
}