{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "AgwfEntryUnreachableTermination.json",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "const": "AGWF035"
        },
        "severity": {
            "type": "string",
            "const": "error"
        },
        "summary": {
            "type": "string",
            "const": "Workflow termination is unreachable"
        },
        "remediation": {
            "type": "string",
            "const": "Step '{0}' in workflow '{1}' chains to '{2}', which is not on the workflow's main flow. A step reached only through its own construct — a fork path, a branch case, a failure or approval handler, or a low-confidence handler chain — is never a main-flow successor, so the saga runs past its declared termination instead of completing."
        },
        "since": {
            "type": "string",
            "const": "2.11.0"
        }
    },
    "required": [
        "id",
        "severity",
        "summary",
        "remediation",
        "since"
    ],
    "description": "AGWF035 — Workflow termination is unreachable (#155)."
}