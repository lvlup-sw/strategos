{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "AgwfEntryUnreachableTermination.json",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "const": "AGWF035"
        },
        "severity": {
            "type": "string",
            "const": "error"
        },
        "summary": {
            "type": "string",
            "const": "Workflow termination is unreachable"
        },
        "remediation": {
            "type": "string",
            "const": "Workflow '{1}' has an unreachable-termination pair '{0}' / '{2}'. Either a main-flow step chains to an off-flow successor, or a rejoin last step never dispatches the declared terminal."
        },
        "since": {
            "type": "string",
            "const": "2.11.0"
        }
    },
    "required": [
        "id",
        "severity",
        "summary",
        "remediation",
        "since"
    ],
    "description": "AGWF035 — Workflow termination is unreachable (#155)."
}