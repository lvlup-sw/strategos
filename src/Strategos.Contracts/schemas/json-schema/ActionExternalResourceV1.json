{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "ActionExternalResourceV1.json",
    "type": "object",
    "properties": {
        "kind": {
            "type": "string",
            "const": "external"
        },
        "name": {
            "type": "string",
            "minLength": 1
        }
    },
    "required": [
        "kind",
        "name"
    ],
    "description": "An external resource declared in a custom predicate's read set."
}