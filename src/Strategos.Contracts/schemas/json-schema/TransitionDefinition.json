{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "TransitionDefinition.json",
    "type": "object",
    "properties": {
        "transitionId": {
            "type": "string",
            "description": "Stable transition identifier."
        },
        "fromStepId": {
            "type": "string",
            "description": "Step id this transition originates from. `@references` (#219) declares the\nendpoint rule: the value must name a step the document declares. This is the\nkernel's referential-integrity obligation for a sequence (#193), stated on\nthe construct `docs/architecture/kernel-v1.md` maps a sequence onto — a\ntransition to nowhere is a workflow with a step that does not exist.",
            "x-strategos-references-v1": {
                "collection": "/steps",
                "idField": "stepId"
            }
        },
        "toStepId": {
            "type": "string",
            "description": "Step id this transition targets. See `fromStepId`.",
            "x-strategos-references-v1": {
                "collection": "/steps",
                "idField": "stepId"
            }
        },
        "isDefault": {
            "type": "boolean",
            "description": "Whether this is the default (fallthrough) transition."
        }
    },
    "required": [
        "transitionId",
        "fromStepId",
        "toStepId",
        "isDefault"
    ],
    "description": "Wire-IR transition — a directed edge between two steps (mirrors the builder's\n`TransitionDefinition`)."
}