{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "PerfMetaV1.json",
    "type": "object",
    "properties": {
        "ms": {
            "type": "integer",
            "minimum": 0,
            "maximum": 2147483647,
            "description": "Wall-clock duration the producer took to compute the response, milliseconds."
        },
        "bytes": {
            "type": "integer",
            "minimum": 0,
            "maximum": 2147483647,
            "description": "Optional byte count processed while computing the response."
        },
        "tokens": {
            "type": "integer",
            "minimum": 0,
            "maximum": 2147483647,
            "description": "Optional token count consumed while computing the response."
        }
    },
    "required": [
        "ms"
    ],
    "description": "Shared `_perf` block for every S-series response envelope (#64).\n\nCarries coarse performance telemetry for the response: wall-clock `ms`, and\noptional `bytes`/`tokens` accounting where the producer measured them. All\ncounters are int32 (not int64) to avoid JSON Schema string coercion."
}