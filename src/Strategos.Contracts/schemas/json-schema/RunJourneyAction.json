{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "RunJourneyAction.json",
    "type": "object",
    "properties": {
        "kind": {
            "type": "string",
            "const": "run-journey",
            "description": "Discriminator: run a workflow journey."
        },
        "journey": {
            "$ref": "WorkflowRef.json",
            "description": "The journey to run, typed as a WorkflowRef (catalog or authored)."
        }
    },
    "required": [
        "kind",
        "journey"
    ],
    "description": "`next_actions` branch (#63): run a catalog/authored journey. Carries a TYPED\n`WorkflowRef` — no string indirection. The consumer (Basileus AgentHost)\nresolves the ref and runs the journey."
}