{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "AgwfEntryLoopWithoutBody.json",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "const": "AGWF014"
        },
        "severity": {
            "type": "string",
            "const": "error"
        },
        "summary": {
            "type": "string",
            "const": "Loop without body"
        },
        "remediation": {
            "type": "string",
            "const": "Loop '{0}' in workflow '{1}' has no steps in its body. A loop must contain at least one step."
        },
        "since": {
            "type": "string",
            "const": "0.2.0"
        }
    },
    "required": [
        "id",
        "severity",
        "summary",
        "remediation",
        "since"
    ],
    "description": "AGWF014 — Loop without body."
}