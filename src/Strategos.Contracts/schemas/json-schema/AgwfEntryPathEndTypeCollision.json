{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "AgwfEntryPathEndTypeCollision.json",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "const": "AGWF036"
        },
        "severity": {
            "type": "string",
            "const": "error"
        },
        "summary": {
            "type": "string",
            "const": "Path-end type collision"
        },
        "remediation": {
            "type": "string",
            "const": "Step type '{0}' is used on more than one exclusive path in workflow '{1}' under distinct instance names. Routing maps key by step type, so instance names do not disambiguate; use distinct step types."
        },
        "since": {
            "type": "string",
            "const": "2.12.0"
        }
    },
    "required": [
        "id",
        "severity",
        "summary",
        "remediation",
        "since"
    ],
    "description": "AGWF036 — Path-end type collision (#190)."
}