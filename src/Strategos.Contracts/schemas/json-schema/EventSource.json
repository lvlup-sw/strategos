{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "EventSource.json",
    "type": "string",
    "enum": [
        "exarchos",
        "basileus"
    ],
    "description": "Source system that emitted an SDLC event. Constrains the envelope's\n`source` field to the two products on the contract boundary."
}