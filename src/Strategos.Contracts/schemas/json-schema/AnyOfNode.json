{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "AnyOfNode.json",
    "type": "object",
    "properties": {
        "kind": {
            "type": "string",
            "const": "any-of"
        },
        "children": {
            "type": "array",
            "items": {
                "$ref": "CheckNode.json"
            },
            "description": "Child checks; at least one must hold (recurses into CheckNode)."
        }
    },
    "required": [
        "kind",
        "children"
    ],
    "description": "Combinator: `any-of` — the disjunction of its child checks. Recurses back into\n`CheckNode` via the `children` array."
}